To set up the project:
1. Run `npm install` to install all dependencies.
2. The tests can then be run using `npm test`. For example, `npm test src/app/EmptyState.test.tsx` was used to verify the setup.
